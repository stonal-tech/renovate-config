name: JSON Schema Check

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: stonal-ubuntu-22.04
    steps:
      - name: Git fetch
        uses: actions/checkout@v4.1.1
      - name: node-setup
        uses: actions/setup-node@v4.0.1
        with:
          node-version: 18
      - name: Installing ajv
        run: |
          cd .github/workflows/
          npm install
      - name: Checking schema
        run: |
          export PATH=$(pwd)/.github/workflows/node_modules/.bin:${PATH}
          set -ex
          curl -s -o .schema.json https://docs.renovatebot.com/renovate-schema.json
          ajv validate --strict=true -s .schema.json -d "*.json"

